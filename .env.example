# COPY TO .env  (the real .env is gitignored)
#
# Postgres credentials — used by both the `postgres` service and the backend's
# DATABASE_URL interpolation in docker-compose.yml.
POSTGRES_USER=riven
POSTGRES_PASSWORD=riven
POSTGRES_DB=riven
